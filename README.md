# spam-email-classification
From-scratch implementations of 7 ML classifiers (Decision Tree, Random Forest, AdaBoost, Naive Bayes, Logistic Regression, Linear SVM, KNN) to detect spam emails on the UCI Spambase dataset, comparing full vs. reduced feature sets
